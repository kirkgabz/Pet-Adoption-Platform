# TODO - Care Tips Feature

- [ ] Create a new Django view + URL for “Care Tips”.
- [ ] Add a new form for species + breed input.
- [ ] Implement a deterministic care-tips generator in the view (no external API).
- [ ] Add a left sidebar item linking to the Care Tips page.
- [ ] Create template `templates/adoption/care_tips.html` with a form and results.
- [ ] Add minimal CSS to match existing tip-card styling.
- [ ] Run Django server / quick sanity checks (migrate not needed if no models added).

